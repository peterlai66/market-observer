# Handoff Protocol

When user says `handoff`:

1. User uploads dev package
2. AI verifies repository structure
3. AI reconciles documentation
4. AI updates missing files
5. AI generates handoff package
