# AI Explain Layer

Purpose:
Convert system output into natural language narration.

Process:
MO payload
↓
GPT narration
↓
2.5 second timeout guard
↓
Fallback built-in summary

Goal:
Ensure LINE webhook always responds.
