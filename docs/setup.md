# Setup

## 初始化
1. `npm install`
2. 建議使用 `npm run mo -- doctor`

## 常用操作
- `npm run mo -- pack`
- `npm run mo -- patch`
- `npm run mo -- release`
- `npm run mo -- update`

## 說明
- 若是修工具鏈，優先使用 `patch`
- 若是完整進版，使用 `update`
